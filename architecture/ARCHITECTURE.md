# 🏗️ Architecture & Diagrams

## System Architecture Overview

```
┌─────────────────────────────────────────────────────────────┐
│                     Client Application                       │
│              (Web/Mobile/Desktop Frontend)                   │
└────────────────────────┬────────────────────────────────────┘
                         │
                         │ HTTP/HTTPS
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                  Internet/Network Layer                       │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
     ┌─────────────────────────────────────┐
     │       Express Server (server.js)     │
     └────────────────┬────────────────────┘
                      │
        ┌─────────────┼─────────────┐
        ▼             ▼             ▼
    ┌────────┐  ┌─────────┐  ┌──────────┐
    │ Helmet │  │  CORS   │  │  Morgan  │
    │ (Sec)  │  │ (CORS)  │  │ (Log)    │
    └────────┘  └─────────┘  └──────────┘
        │             │             │
        └─────────────┼─────────────┘
                      │
                      ▼
         ┌────────────────────────┐
         │  Rate Limiter (100/15) │
         └────────────┬───────────┘
                      │
        ┌─────────────┴──────────────┐
        │                            │
        ▼                            ▼
   ┌──────────────┐           ┌──────────────┐
   │ /health      │           │ /api/auth/*  │
   │ (public)     │           │ (protected)  │
   └──────────────┘           └──────┬───────┘
                                     │
                    ┌────────────────┴────────────────┐
                    │                                 │
                    ▼                                 ▼
         ┌──────────────────────┐      ┌──────────────────────┐
         │  JWT Verify          │      │  Input Validation    │
         │  (authenticateToken) │      │  (Business Logic)    │
         └──────────┬───────────┘      └──────────┬───────────┘
                    │                             │
                    └─────────────┬───────────────┘
                                  │
                    ┌─────────────┴─────────────┐
                    │                           │
                    ▼                           ▼
        ┌───────────────────┐      ┌──────────────────┐
        │ authController.js │      │  Parameterized   │
        │ - Validate input  │      │  Queries (db.js) │
        │ - Query DB        │      │  - SQL Injection │
        │ - Verify password │      │    Prevention    │
        │ - Generate JWT    │      └────────┬─────────┘
        └────────┬──────────┘               │
                 │                         │
                 └────────────┬────────────┘
                              │
                              ▼
                    ┌──────────────────────┐
                    │  PostgreSQL Database │
                    │  - students table    │
                    │  - users table       │
                    │  - Connection Pool   │
                    └──────────────────────┘
```

---

## Request Lifecycle - Login Flow

```
1. CLIENT SENDS REQUEST
   ┌────────────────────────────────┐
   │ POST /api/auth/login           │
   │ Content-Type: application/json │
   │                                │
   │ {                              │
   │   "userType": "student",       │
   │   "id": "30412010101234",      │
   │   "password": "123456"         │
   │ }                              │
   └────────────────────┬───────────┘
                        │
                        ▼
2. SERVER MIDDLEWARE PROCESSING
   ┌────────────────────────────────┐
   │ Security Middleware            │
   ├────────────────────────────────┤
   │ ✓ Helmet → Add security headers│
   │ ✓ CORS → Check origin          │
   │ ✓ Morgan → Log request         │
   │ ✓ Body Parser → Parse JSON     │
   │ ✓ Rate Limiter → Check quota   │
   └────────────────────┬───────────┘
                        │
                        ▼
3. ROUTE MATCHING (routes/auth.js)
   ┌────────────────────────────────┐
   │ Router.post('/login', callback) │
   │ Match → Call authController    │
   └────────────────────┬───────────┘
                        │
                        ▼
4. CONTROLLER PROCESSING (authController.js)
   ┌────────────────────────────────────┐
   │ Step 1: Validate Input             │
   │ • Check required fields            │
   │ • Check userType is valid          │
   │ → Reject if invalid                │
   │                                    │
   │ Step 2: Query Database             │
   │ • If student: SELECT FROM students │
   │   WHERE national_id = $1 (safe!)   │
   │ • If admin: SELECT FROM users      │
   │   WHERE username = $1 (safe!)      │
   │ → Return user if exists            │
   │                                    │
   │ Step 3: Verify Password            │
   │ • Use bcrypt.compare()             │
   │ • Plain password vs hashed         │
   │ → Return error if no match         │
   │                                    │
   │ Step 4: Generate JWT Token         │
   │ • Create payload: {id, role}       │
   │ • Sign with JWT_SECRET             │
   │ • Expire in 1 day                  │
   │                                    │
   │ Step 5: Return Success Response    │
   │ {                                  │
   │   "success": true,                 │
   │   "token": "eyJhbGc...",           │
   │   "user": { ... }                  │
   │ }                                  │
   └────────────────────┬───────────────┘
                        │
                        ▼
5. DATABASE INTERACTION (db.js)
   ┌────────────────────────────────┐
   │ Connection Pool Management     │
   ├────────────────────────────────┤
   │ SELECT * FROM students         │
   │ WHERE national_id = $1         │
   │ [30412010101234]               │
   │                                │
   │ → Returns user record or null  │
   └────────────────────┬───────────┘
                        │
                        ▼
6. RESPONSE SENT TO CLIENT
   ┌────────────────────────────────┐
   │ HTTP/200 OK                    │
   │ Content-Type: application/json │
   │                                │
   │ {                              │
   │   "success": true,             │
   │   "token": "eyJ...",           │
   │   "user": {                    │
   │     "id": 1,                   │
   │     "name": "أحمد",             │
   │     "role": "student"          │
   │   }                            │
   │ }                              │
   └────────────────────────────────┘
```

---

## Middleware Stack Order

```
Entry Point: HTTP Request
    │
    ▼
1. helmet() 
   ├─ Add security headers
   ├─ Disable X-Powered-By
   └─ Prevent MIME sniffing
    │
    ▼
2. cors()
   ├─ Check origin header
   ├─ Add CORS headers
   └─ Allow cross-domain requests
    │
    ▼
3. express.json()
   ├─ Parse JSON body
   ├─ Limit to 10KB
   └─ Create req.body
    │
    ▼
4. morgan('dev')
   ├─ Log HTTP method
   ├─ Log path & status
   └─ Log response time
    │
    ▼
5. rateLimit()
   ├─ Count requests per IP
   ├─ Check against limit
   └─ Return 429 if exceeded
    │
    ▼
6. Route Matching
   ├─ POST /api/auth/login
   ├─ GET /health
   └─ POST /api/other
    │
    ▼
7. authenticateToken (protected routes only)
   ├─ Extract Authorization header
   ├─ Verify JWT signature
   └─ Attach req.user
    │
    ▼
8. authorizeRole (protected routes only)
   ├─ Check req.user.role
   ├─ Verify against allowed roles
   └─ Return 403 if unauthorized
    │
    ▼
9. Route Handler (Controller)
   ├─ Business logic
   ├─ Database queries
   └─ Response creation
    │
    ▼
Exit: HTTP Response → Client
```

---

## Database Schema Diagram

```
┌─────────────────────────────┐
│      STUDENTS TABLE         │
├─────────────────────────────┤
│ id (PK)                     │
│ ├─ Type: SERIAL             │
│ ├─ Primary Key              │
│ └─ Auto-increment           │
├─────────────────────────────┤
│ national_id (UNIQUE)        │
│ ├─ Type: VARCHAR(14)        │
│ ├─ Unique constraint        │
│ ├─ Example: 30412010101234  │
│ └─ Used for login           │
├─────────────────────────────┤
│ password_hash               │
│ ├─ Type: VARCHAR(255)       │
│ ├─ bcrypt hashed            │
│ ├─ Never plain text         │
│ └─ Verified with bcrypt     │
├─────────────────────────────┤
│ full_name                   │
│ ├─ Type: VARCHAR(150)       │
│ └─ Student's full name      │
├─────────────────────────────┤
│ created_at (Optional)       │
│ ├─ Type: TIMESTAMP          │
│ └─ Record creation time     │
├─────────────────────────────┤
│ updated_at (Optional)       │
│ ├─ Type: TIMESTAMP          │
│ └─ Last update time         │
└─────────────────────────────┘

┌──────────────────────────────┐
│       USERS TABLE (Admin)    │
├──────────────────────────────┤
│ id (PK)                      │
│ ├─ Type: SERIAL              │
│ ├─ Primary Key               │
│ └─ Auto-increment            │
├──────────────────────────────┤
│ username (UNIQUE)            │
│ ├─ Type: VARCHAR(100)        │
│ ├─ Unique constraint         │
│ ├─ Example: admin            │
│ └─ Used for login            │
├──────────────────────────────┤
│ password_hash                │
│ ├─ Type: VARCHAR(255)        │
│ ├─ bcrypt hashed             │
│ ├─ Never plain text          │
│ └─ Verified with bcrypt      │
├──────────────────────────────┤
│ full_name                    │
│ ├─ Type: VARCHAR(150)        │
│ └─ Admin's full name         │
├──────────────────────────────┤
│ role                         │
│ ├─ Type: VARCHAR(50)         │
│ ├─ Default: 'admin'          │
│ ├─ Examples: admin, manager  │
│ └─ Used for authorization    │
├──────────────────────────────┤
│ created_at & updated_at      │
│ └─ Same as students table    │
└──────────────────────────────┘
```

---

## Authentication Flow Diagram

```
                    CLIENT
                      │
                      │ 1. POST /login
                      ├──────────────────────┐
                      │  {userType, id, pwd} │
                      └──────────┬───────────┘
                                 │
                                 ▼
                    ┌────────────────────────┐
                    │ Server: Validate Input │
                    │ ✓ All fields present?  │
                    │ ✓ Valid userType?      │
                    └────────────┬───────────┘
                                 │
                    ┌────────────┴────────────┐
                    │                         │
                    ▼                         ▼
            ✓ Valid              ✗ Invalid
                │                    │
                ▼                    ▼
        Query Database      Return Error (400)
        ┌─────────────┐         │
        │ Student DB  │         │
        │ Admin DB    │         │
        └──────┬──────┘         │
               │                │
               ▼                │
        User Found?             │
        ┌────────────┐          │
        │ Yes  │  No │          │
        └─┬──────────┘          │
          │         │           │
          │         ▼           │
          │    Return (401)     │
          │         │           │
          │         └──────┬────┘
          │                │
          ▼                ▼
    Verify Password    ERROR: Invalid
    ├─ bcrypt.compare  ┌─────────────────┐
    ├─ Plain vs Hash   │ 401 Unauthorized│
    │                  │ "Wrong password"│
    ├─ Match? YES / NO │ (generic msg)   │
    │         │    │   └─────────────────┘
    │         │    │
    │         │    ▼
    │         │  Return (401)
    │         │    │
    │         │    └──────────────┐
    │         │                   │
    │         │                   ▼
    │         │               ERROR
    │         │               RESPONSE
    │         │
    │         ▼ (No Match)
    │    Return (401)
    │         │
    │         └──────────────────┐
    │                            │
    ▼ (Match)                    │
Generate JWT Token         ┌─────┴──────────┐
├─ Payload: {id, role}    │ HTTP 401       │
├─ Sign: JWT_SECRET        │ msg: Invalid   │
├─ Expire: 1d              │ credentials    │
└──────┬──────────────────┘                │
       │                                    │
       ▼                                    ▼
  Return Success Response              CLIENT
  ┌──────────────────────┐
  │ HTTP 200 OK          │
  │ {                    │
  │   token: "eyJ...",   │
  │   user: {...}        │
  │ }                    │
  └──────┬───────────────┘
         │
         ▼
     CLIENT
   (Store Token)
       │
       ▼
  Use Token for
  Future Requests:
  Authorization:
  Bearer <token>
```

---

## Data Flow Diagram

```
┌─────────────────────────────────────────────────────────┐
│ CLIENT APPLICATION (Web/Mobile/Desktop)                │
│                                                         │
│ User fills login form:                                 │
│ - National ID or Username                             │
│ - Password                                             │
│ - User Type (Student/Admin)                           │
└───────────────────┬─────────────────────────────────────┘
                    │
                    │ 1. Send request
                    ▼
┌─────────────────────────────────────────────────────────┐
│ EXPRESS SERVER (server.js)                             │
├─────────────────────────────────────────────────────────┤
│                                                         │
│ 1. Request arrives                                     │
│    └─→ Helmet adds security headers                   │
│    └─→ CORS checks origin                             │
│    └─→ Morgan logs request                            │
│    └─→ Body parser extracts JSON                      │
│    └─→ Rate limiter checks quota                      │
│                                                         │
│ 2. Route matching → /api/auth/login                   │
│    └─→ Pass to authController.login()                │
│                                                         │
│ 3. Controller logic (authController.js)               │
│    └─→ Validate input fields                          │
│    └─→ Determine student vs admin                     │
│    └─→ Call database.query()                          │
│                                                         │
│ 4. Password verification                              │
│    └─→ bcrypt.compare(plaintext, hashed)              │
│    └─→ Check if match                                 │
│                                                         │
│ 5. Generate JWT token                                 │
│    └─→ jwt.sign() with payload & secret               │
│    └─→ Set expiration to 1 day                        │
│                                                         │
│ 6. Build response                                      │
│    └─→ Include token                                   │
│    └─→ Include user info (no password!)              │
│                                                         │
└──────────────┬─────────────────────────────────────────┘
               │
               │ 2. Database query
               ▼
┌─────────────────────────────────────────────────────────┐
│ DATABASE CONNECTION (db.js)                            │
├─────────────────────────────────────────────────────────┤
│                                                         │
│ Connection Pool Management:                            │
│ - Reuse existing connections                          │
│ - Create new if needed (up to max)                    │
│ - Handle connection errors                            │
│                                                         │
│ Query Execution:                                       │
│ SELECT * FROM students                                │
│ WHERE national_id = $1                                │
│ [$userInput]                                          │
│                                                         │
│ WHY $1?                                                │
│ └─→ Prevents SQL injection                            │
│ └─→ Parameter is treated as data, not SQL             │
│ └─→ Database driver handles escaping                  │
│                                                         │
│ Return:                                                │
│ - User record if found                                │
│ - Null if not found                                   │
│                                                         │
└──────────────┬─────────────────────────────────────────┘
               │
               │ 3. Student/User Data
               ▼
┌─────────────────────────────────────────────────────────┐
│ POSTGRESQL DATABASES                                   │
├─────────────────────────────────────────────────────────┤
│                                                         │
│ STUDENTS TABLE                                         │
│ id │ national_id   │ password_hash  │ full_name       │
│ ───┼───────────────┼────────────────┼─────────────────│
│ 1  │ 30412010..    │ $2b$10$abc...  │ أحمد حسن محمد    │
│    │               │                │                 │
│    │ (hashed, not  │ (hashed with   │ (stored in      │
│    │  plain text)  │  bcrypt salt)  │  Arabic)        │
│                                                         │
│ USERS TABLE                                            │
│ id │ username │ password_hash  │ full_name │ role     │
│ ───┼──────────┼────────────────┼───────────┼──────────│
│ 1  │ admin    │ $2b$10$xyz...  │ Admin     │ admin    │
│                                                         │
└──────────────┬─────────────────────────────────────────┘
               │
               │ 4. User record (or null)
               ▼
┌─────────────────────────────────────────────────────────┐
│ BACK TO SERVER - PASSWORD VERIFICATION                │
├─────────────────────────────────────────────────────────┤
│                                                         │
│ bcrypt.compare():                                      │
│ ┌──────────────┐         ┌─────────────────┐          │
│ │ Plain Text   │         │ Hashed From DB  │          │
│ │ "123456"     │ ─────→  │ $2b$10$abc...   │          │
│ └──────────────┘         └────────┬────────┘          │
│                                   │                    │
│                                   ▼                    │
│                          ┌─────────────────┐          │
│                          │ Match? YES / NO │          │
│                          └────────┬────────┘          │
│                                   │                    │
│              ┌────────────────────┼────────────────┐  │
│              │                    │                │  │
│              ▼ (YES)              ▼ (NO)           │  │
│          Continue to       Return HTTP 401    │  │
│          JWT generation    "Invalid creds"    │  │
│                                                │  │
└─────────────────────────────────────────────────┼──┘
                                                  │
                ┌─────────────────────────────────┘
                │
                ▼
    ┌──────────────────────────────┐
    │ Generate JWT Token           │
    ├──────────────────────────────┤
    │ Payload:                     │
    │ {                            │
    │   id: 1,                     │
    │   role: "student",           │
    │   iat: 1674567890,           │
    │   exp: 1674654290            │
    │ }                            │
    │                              │
    │ Signing:                     │
    │ HMAC-SHA256(                 │
    │   payload,                   │
    │   JWT_SECRET                 │
    │ )                            │
    │                              │
    │ Result:                      │
    │ eyJhbGciOiJIUzI1NiIs...      │
    └──────────────┬───────────────┘
                   │
                   ▼
    ┌──────────────────────────────┐
    │ Return JSON Response         │
    │                              │
    │ {                            │
    │   "success": true,           │
    │   "token": "eyJ...",         │
    │   "user": {                  │
    │     "id": 1,                 │
    │     "name": "أحمد",           │
    │     "role": "student"        │
    │   },                         │
    │   "message": "تم..."         │
    │ }                            │
    └──────────────┬───────────────┘
                   │
                   │ 3. Response (HTTP 200)
                   ▼
    ┌──────────────────────────────┐
    │ CLIENT STORES TOKEN          │
    │                              │
    │ localStorage.setItem(        │
    │   'token',                   │
    │   'eyJhbGc...'               │
    │ )                            │
    │                              │
    │ Future requests:             │
    │ Authorization: Bearer eyJ... │
    └──────────────────────────────┘
```

---

## Security Layers Visualization

```
┌──────────────────────────────────────────────────────────────┐
│                    LAYER 1: TRANSPORT                        │
│                 (HTTPS/TLS in production)                   │
│                                                              │
│            Encrypts data in transit                         │
└──────────┬───────────────────────────────────────────────────┘
           │
           ▼
┌──────────────────────────────────────────────────────────────┐
│                    LAYER 2: HTTP HEADERS                    │
│              (Helmet.js - Security Headers)                 │
│                                                              │
│ ✓ X-Content-Type-Options: nosniff (MIME sniffing)          │
│ ✓ X-Frame-Options: DENY (Clickjacking)                     │
│ ✓ Strict-Transport-Security (HTTPS)                        │
│ ✓ Content-Security-Policy (XSS)                            │
│ ✓ X-XSS-Protection                                          │
└──────────┬───────────────────────────────────────────────────┘
           │
           ▼
┌──────────────────────────────────────────────────────────────┐
│                    LAYER 3: CORS                            │
│            (Cross-Origin Resource Sharing)                  │
│                                                              │
│ ✓ Check Origin header                                       │
│ ✓ Allow/Deny cross-domain requests                         │
└──────────┬───────────────────────────────────────────────────┘
           │
           ▼
┌──────────────────────────────────────────────────────────────┐
│                    LAYER 4: RATE LIMITING                   │
│           (DDoS & Brute Force Protection)                   │
│                                                              │
│ ✓ 100 requests per 15 minutes per IP                        │
│ ✓ Blocks excessive requests                                 │
│ ✓ Prevents password guessing                                │
└──────────┬───────────────────────────────────────────────────┘
           │
           ▼
┌──────────────────────────────────────────────────────────────┐
│                   LAYER 5: INPUT VALIDATION                 │
│           (Prevent Invalid/Malicious Data)                  │
│                                                              │
│ ✓ Check required fields exist                               │
│ ✓ Validate data types                                       │
│ ✓ Sanitize special characters                               │
└──────────┬───────────────────────────────────────────────────┘
           │
           ▼
┌──────────────────────────────────────────────────────────────┐
│                 LAYER 6: PARAMETERIZED QUERIES              │
│              (SQL Injection Prevention)                     │
│                                                              │
│ ✓ User input NEVER touches SQL                              │
│ ✓ Use $1, $2, $3 placeholders                               │
│ ✓ Database driver handles escaping                          │
│                                                              │
│ Example:                                                    │
│ VULNERABLE:  `WHERE id = '${input}'`                       │
│ SECURE:      'WHERE id = $1' with [input]                  │
└──────────┬───────────────────────────────────────────────────┘
           │
           ▼
┌──────────────────────────────────────────────────────────────┐
│                 LAYER 7: PASSWORD HASHING                   │
│               (bcrypt - Irreversible Hash)                  │
│                                                              │
│ ✓ Passwords never stored plain                              │
│ ✓ Each password has unique salt                             │
│ ✓ Takes ~100ms per comparison (slow brute force)            │
│ ✓ Rainbow tables ineffective                                │
│                                                              │
│ Plain: 123456 ──┐                                           │
│                 ├─→ bcrypt ─→ $2b$10$abc...xyz              │
│ Salt: random ──┘                                            │
└──────────┬───────────────────────────────────────────────────┘
           │
           ▼
┌──────────────────────────────────────────────────────────────┐
│                   LAYER 8: JWT TOKENS                       │
│         (Stateless Authentication, Auto-Expire)            │
│                                                              │
│ ✓ Token signed with secret (can't forge)                    │
│ ✓ Expires automatically (1 day)                             │
│ ✓ Contains user info (id, role)                             │
│ ✓ Verified on each protected request                        │
│                                                              │
│ Structure:                                                  │
│ header.payload.signature                                    │
│                                                              │
│ Payload contains:                                           │
│ {                                                           │
│   id: 1,              ← User ID                             │
│   role: "student",    ← Authorization                       │
│   iat: 1674567890,    ← Issued at                           │
│   exp: 1674654290     ← Expires at                          │
│ }                                                           │
└──────────┬───────────────────────────────────────────────────┘
           │
           ▼
┌──────────────────────────────────────────────────────────────┐
│              LAYER 9: ERROR HANDLING                        │
│      (No Information Leaks, Proper Status Codes)           │
│                                                              │
│ ✓ Generic error messages to client                          │
│ ✓ Detailed logging server-side                              │
│ ✓ Proper HTTP status codes (400, 401, 403, 500)            │
│ ✓ No stack traces to client                                 │
│                                                              │
│ Example:                                                    │
│ CLIENT:  "Invalid credentials" (generic)                   │
│ SERVER:  Log full error for debugging                      │
└──────────────────────────────────────────────────────────────┘
```

---

## File Dependency Graph

```
server.js (Entry Point)
├── Requires: express, helmet, cors, rate-limit, morgan, dotenv
├── Requires: routes/auth
└── Uses: Middleware setup

routes/auth.js
├── Requires: express, controllers/authController
└── Defines: POST /api/auth/login

controllers/authController.js
├── Requires: db, bcrypt, jsonwebtoken, dotenv
├── Uses: db.query() for database
├── Uses: bcrypt.compare() for password
└── Uses: jwt.sign() for tokens

db.js
├── Requires: pg, dotenv
├── Exports: query() method
└── Manages: Connection pool

middleware/auth.js
├── Requires: jsonwebtoken, dotenv
├── Exports: authenticateToken, authorizeRole, verifyOwnership
└── Uses: jwt.verify() for validation

setupDB.js
├── Requires: pg, bcrypt, dotenv
├── Creates: Database tables
└── Seeds: Test data

.env
├── Contains: Secrets & configuration
├── DB credentials
├── JWT_SECRET
└── Port & environment
```

---

## Deployment Architecture (Future)

```
┌──────────────────────────────────────────────────────────┐
│                    PRODUCTION                            │
├──────────────────────────────────────────────────────────┤
│                                                          │
│ ┌────────────────────────────────────────────────────┐  │
│ │           Users/Clients                            │  │
│ └──────────────────┬─────────────────────────────────┘  │
│                    │                                     │
│                    │ HTTPS                              │
│                    ▼                                     │
│ ┌────────────────────────────────────────────────────┐  │
│ │           Load Balancer                            │  │
│ │     (Distribute traffic)                           │  │
│ └──────────┬──────────────────┬───────────────────────┘  │
│            │                  │                          │
│            ▼                  ▼                          │
│ ┌────────────────────┐  ┌────────────────────┐          │
│ │  API Server 1      │  │  API Server 2      │          │
│ │ Node.js Express    │  │ Node.js Express    │          │
│ │ (Port 3000)        │  │ (Port 3000)        │          │
│ └────────┬───────────┘  └────────┬───────────┘          │
│          │                       │                       │
│          │                       │                       │
│          └───────────┬───────────┘                       │
│                      │                                   │
│                      │ Connection Pool                   │
│                      ▼                                   │
│          ┌─────────────────────┐                        │
│          │  PostgreSQL DB      │                        │
│          │ (Replicated/Backed) │                        │
│          │ - Database          │                        │
│          │ - Users             │                        │
│          │ - Students          │                        │
│          └─────────────────────┘                        │
│                                                          │
│ ┌────────────────────────────────────────────────────┐  │
│ │           Monitoring & Logging                     │  │
│ │ - Sentry (Error tracking)                         │  │
│ │ - ELK Stack (Log aggregation)                     │  │
│ │ - Prometheus (Metrics)                            │  │
│ │ - Datadog (Monitoring)                            │  │
│ └────────────────────────────────────────────────────┘  │
│                                                          │
│ ┌────────────────────────────────────────────────────┐  │
│ │           Backups & Recovery                       │  │
│ │ - Daily database backups                          │  │
│ │ - Disaster recovery plan                          │  │
│ └────────────────────────────────────────────────────┘  │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

---

**Created:** January 25, 2026  
**Version:** 1.0.0  
**For:** University Housing System API
